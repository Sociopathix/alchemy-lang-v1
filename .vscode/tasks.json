{
	"version": "2.0.0",
	"tasks": [
		{
            "label": "g++.exe build active file",
            "type": "shell",
            "command": "g++ -g ${workspaceFolder}/src/**.cpp ${workspaceFolder}/include/**.hpp -o Alchemy.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
			],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
	]
}